{
  "name": "caresync-pro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-server": "cd server-side && npm install",
    "start-server": "cd server-side && set NODE_ENV=${NODE_ENV:-development} node app.js",
    "install-client": "cd client-side && npm install",
    "build-client": "cd client-side && npm run build",
    "start-client": "cd client-side && npm run start"
  },
  "author": "",
  "license": "ISC"
}